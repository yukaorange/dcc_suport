# CLAUDE.md

## プロジェクトの前提

dcc supportプロジェクト立ち上げに至るまでの経緯 `docs/memory/`の思想を把握した上で作業に臨むこと。

## 使用技術、ライブラリ

詳細は `docs/README.md` の技術スタック概要を参照。

## 開発コマンド

<!-- - `bun run dev` - 開発サーバー -->

## ドキュメント

作業開始時には下記のREADME.mdをもとにドキュメントを把握してください。

@~/docs/README.md

### docs更新ルール

#### 提案タイミング

以下の状況で、ドキュメント更新を提案してください：

1. **エラーや問題を解決した時**
2. **効率的な実装パターンを発見した時**
3. **新しいAPI/ライブラリの使用方法を確立した時**
4. **既存ドキュメントの情報が古い/不正確だと判明した時**
5. **頻繁に参照される情報を発見した時**
6. **コードレビューの修正を終わらせた時**

### 既存ドキュメントとの連携

- 既存の記載形式やスタイルを踏襲すること
- 関連する既存内容がある場合は参照を明記すること
- 日付（YYYY-MM-DD形式）を含めて更新履歴を残すこと

#### 提案フォーマット

💡 ドキュメント更新の提案： [状況の説明]
【更新内容】 [具体的な追加/修正内容]
【更新候補】
[ファイルパス1] - [理由]
[ファイルパス2] - [理由]
新規ファイル作成 - [理由]

### 重要な制約

1. **ユーザーの承認なしにファイルを更新しない**
2. **機密情報（APIキー、パスワード等）は記録しない**
3. **プロジェクトの慣習やスタイルガイドに従う**

### ドキュメント提案と分割管理

claude codeのrule/skillsまわりで不足しているドキュメントに気づいた時は提案して欲しいです。
CLAUDE.mdが肥大化することを防ぐため、適切にファイルを分割してください。

- **常時適用すべきルール** → `.claude/rules/` に配置（毎セッション自動読込）
- **タスク手順書・ワークフロー** → `.claude/skills/<name>/SKILL.md` に配置（`/name` で呼出時のみ読込）
- **プロジェクト仕様・設計** → `docs/` に配置

## rule

- ALWAYS use bun instead of npm. (DO NOT use dotenv, env)
- NEVER include comments which describes a code line-by-line
- コメントを書く場合は「何をしているか」ではなく「なぜそうしているか」を書くこと

## other

- ブランチ名はissueタイトルのプレフィックスと同期しています(e.g. SCR-XX)。よって、「いまからSCR-01に着手します」などの指示は、そのissueに取り掛かるという意図であることを理解しなさい
- プロジェクトリーダー（私）は、フロントエンドにはある程度造形があるが、バックエンドやインフラ、CI/CD、github、dockerコンテナといった部分については基礎の理解があるだけで経験に乏しいので、その辺りの実装やプランニングについては私に対する説明をより丁寧におこうことを心がけること
